# RELEASE-P0-2026-06-05

## Release Decision: HOLD

## Summary
로컬 정적 검증, 빌드, 함수 빌드, 번들 비밀키 스캔, E2E, 유닛 커버리지는 통과했다.

출시는 보류한다. Firebase 배포 증거, P0-003 법무 승인, 정식 MED gate 근거가 부족하다.

## Gate Status

| Gate | Status | Evidence |
|---|---|---|
| STATIC | PASS | `npm run build`, `npm run scan:secrets`, `npm run scan:bundle` passed |
| FUNCTIONS BUILD | PASS | `npm --prefix functions run build` passed |
| SEC / P0-005 | HOLD | Firebase CLI runs, but `.firebaserc` uses placeholder project ID |
| PRIV / P0-002 | PASS locally | `symptomHistory` server access denied; local-only history retained |
| LEGAL / P0-003 | HOLD | `docs/reviews/LEGAL-P0-003-2026-06-05.md` recommends disabling OTC display for first release |
| MED | HOLD | `docs/reviews/MED-GATE-2026-06-05.md`; local tests pass, regression corpus below launch threshold |
| QA | PASS locally | E2E 4 passed; unit coverage 6 files, 24 tests passed |
| RELEASE | HOLD | Missing deployed Firebase release identifiers and rollback target |

## Commands Run

```text
npm run build
npm --prefix functions run build
npm run scan:bundle
npm run scan:secrets
npx firebase-tools --version
npx firebase-tools projects:list
npm run test:e2e
npx vitest run --coverage --pool=threads --no-file-parallelism --maxWorkers=1
```

## Key Results
- `firebase-tools` version: 15.19.1.
- `npx firebase-tools projects:list` failed because project ID is `REPLACE_WITH_YOUR_FIREBASE_PROJECT_ID`.
- `npm run test:coverage` without worker limits failed due to Vitest worker startup timeout in this environment.
- Worker-limited Vitest command passed.
- E2E passed: 4 tests.
- Unit coverage passed: 6 files, 24 tests.

## Remaining Blockers

### P0-005 Firebase Deployment Evidence
Required:
- replace `.firebaserc` placeholder with the real Firebase project ID.
- confirm Firebase authenticated account access.
- deploy or verify Hosting, Functions, and Firestore Rules.
- confirm `GEMINI_API_KEY` is configured as a Functions secret.
- capture Hosting release ID, Functions revision, and Firestore Rules release timestamp.

### P0-003 Legal Decision
Required:
- decide whether OTC display is disabled for first release.
- obtain legal approval if OTC names remain visible.
- confirm pharmacy UI has no purchase, stock, price, recommendation, or partner routing language.

### MED Gate
Required:
- expand redFlag suite to 50+ cases.
- expand medical regression prompts to 100+ cases.
- prove redFlag miss-rate below 1%.
- enforce MFDS OTC master or disable OTC display.

## Rollback Plan Status
Incomplete.

Rollback plan requires actual deployed release identifiers:
- Firebase Hosting previous release version.
- Cloud Functions previous revision.
- Firestore Rules previous ruleset release.

## Next Command

```text
Set Firebase project ID and verify deployed Firebase resources
```
