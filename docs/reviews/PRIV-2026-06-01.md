# PRIV-gate · 2026-06-01 결과 보고

> Reviewer: privacy-officer (자동 디스패치, R2 트리거)
> 범위: PIPA §15 / §22 / §22-2 / §23 / §28-8 / §35 / §35-2 / §36, 위치정보법 §15
> 결정: **🔴 BLOCK → 🟢 PASS-CONDITIONAL** (서버 DSR 함수 배포 시 완전 PASS)

---

## 1. 해결된 항목

| ID | 요구사항 | 해결 방법 |
|---|---|---|
| R-002 | "수집/저장하지 않습니다" 카피 ↔ Firestore 저장 모순 | ConsentGate 가 별도 동의 받지 않으면 `symptomHistory` 저장 안 함 (App.tsx 가드 + Firestore Rules 이중 방어) |
| -- | 별도 동의 화면 (PIPA §23) | `ConsentGate.tsx` — 필수 4종(pii/sensitiveHealth/overseasTransfer/location) + 선택 2종(marketing/analytics) + 항목별 펼침 상세 |
| -- | 14세 미만 차단 (PIPA §22-2) | `isAdult(year,month,day)` 검증, 동의 화면에서 생년월일 입력 강제 |
| -- | 국외이전 동의 (PIPA §28-8) | overseasTransfer 필수, 상세에 이전국·이전받는자·목적·기간 명시 |
| -- | 위치정보 별도 동의 (위치정보법 §15) | location 필수 항목 분리 |
| -- | DSR 4종 (PIPA §35/§35-2/§36) | `consentService.ts` + `functions/src/dsr.ts` (access/rectify/erase/export) |
| -- | DSR UI | `PrivacySettings.tsx` (탭 신설) — JSON 다운로드 / 동의 철회 / 탈퇴 |
| -- | Audit log 5년 (PIPA 시행령 §16) | `dsr.ts` 의 audit() — `ttlAt` 필드로 5년 retention 표시, 클라이언트 read/write 금지 |
| R-010 | 캐시 키 평문 | base64 인코딩으로 완화 (SHA-256은 후속) |

## 2. 게이트별 자동화 강제

| 레이어 | 강제 방식 |
|---|---|
| 클라이언트 | `App.tsx` 의 `needsConsent` 가드가 ConsentGate 모달 강제 노출 |
| 클라이언트 | `App.tsx:handleAnalyze` 가 `consent.items.sensitiveHealth + version 일치` 검증 후에만 saveSearchRecord |
| Firestore Rules | symptomHistory create 시 `users/{uid}/consents/{request.resource.data.consentVersion}` 존재 + `items.sensitiveHealth==true` 확인 (서버측 final 가드) |
| Firestore Rules | consents create 시 acceptedAt = request.time, isAdult=true, 필수 4종 모두 true 검증 |
| 서버 (DSR) | Firebase Auth ID 토큰 검증 (Authorization: Bearer), uid 위변조 차단 |
| 서버 (DSR) | erase 시 auth.deleteUser 까지 — 좀비 계정 방지 |
| 감사 | 모든 DSR 요청 auditLogs 컬렉션에 append (uid, type, ok, requestId, affected, err) |

## 3. 동의 매트릭스 반영 (docs/legal/consent-matrix.md 동기화)

✅ `consents/{CONSENT_VERSION}` 컬렉션이 매트릭스의 모든 행을 1:1 매핑
✅ `auditLogs.ttlAt` 으로 5년 보존 명시
⚠️ Storage 컬렉션 추가 시 매트릭스 새 행 추가 필요 (현재 없음)

## 4. 미해결 / 후속 항목

| 항목 | 우선 | 책임 |
|---|---|---|
| Hosting 측 `privacy@medi-curator.example` 도메인 / 실제 분쟁조정 연락처 | P1 | product-strategist |
| `localStorage` 의 language/theme 도 동의 매트릭스 row 추가 (사실상 cookie-equivalent) | P2 | privacy-officer |
| 캐시 키 SHA-256 (현재 base64) | P2 | api-engineer |
| 국외이전 받는자 영문법인명 / 미국 주소 / 책임자 이메일 명시 (PIPA 시행령 §29-3) | P0 | legal-advisor |
| 정정권 UI — displayName 외 항목 (현재 patch 로 displayName만 처리) | P2 | ux-designer + api-engineer |
| 동의 버전 bump 시 사용자 알림 (배너 + 재동의 모달) | P1 | ux-designer |

## 5. 회귀 테스트

```
tests/unit/consent.test.ts        4 tests (필수4종/일부거부/Record/isAdult 만14세 경계)
tests/unit/SymptomInput.test.tsx  4 tests (R-005 회귀)
tests/unit/schemas.test.ts        4 tests (CurationResult Zod)
tests/unit/geminiService.test.ts  4 tests (프록시 호출)
tests/unit/pharmacyService.test.ts 2 tests
                                 ─────
                                 18 passed
```

## 6. 검증 명령

```
✓ tsc -b --noEmit             0 error
✓ functions/tsc               0 error (curate + dsr)
✓ vitest run                  18/18 passed
✓ scan-secrets.mjs            PASS
─ npm run lint                3 errors / 4 warnings — 모두 react-refresh / exhaustive-deps (PRIV 무관, STATIC 후속)
```

## 7. 결정

`legal-advisor` 의 §28-8 받는자 명시(P0) 완료 + 서버 dsr 배포 시 **🟢 PASS**.
지금 시점에선 **🟢 PASS-CONDITIONAL** (코드는 모두 합의, 인프라 배포만 남음).
